input_line = input()

sk = list(input())
a = list(input())
b = list(input())

def compare(x):
    for s in sk:
        if s == x[0]:
            del(x[0])
            if x == []:
                break
            
compare(a)
compare(b)

print(len(a),len(b))
